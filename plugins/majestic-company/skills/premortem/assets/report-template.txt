<!doctype html>
<!--
  Premortem report template. Substitute the {{...}} placeholders before writing.
  Self-contained — no external CSS, no external fonts, no external images.

  Placeholders:
    {{TITLE}}            — short label of what was premortemed
    {{TIMESTAMP}}        — human-readable timestamp
    {{WHAT}}             — one-sentence description
    {{WHO}}              — audience / stakeholders
    {{SUCCESS}}          — what success looks like
    {{N_FAILURES}}       — integer
    {{MOST_LIKELY}}      — paragraph (most likely failure + why)
    {{MOST_DANGEROUS}}   — paragraph (most dangerous failure + why)
    {{HIDDEN_ASSUMPTION}}— paragraph (the unquestioned assumption)
    {{REVISED_PLAN}}     — <ul><li> items, each concrete action
    {{CHECKLIST}}        — <ul><li> items, 3-5 verifiable preflight items
    {{FAILURE_CARDS}}    — repeated card blocks (template below)

  Per-failure card template (one per failure, distinct accent colors cycled):
    <article class="card" style="--accent: #ff6b6b;">
      <header>
        <span class="badge severity-high">Severity: High</span>
        <h3>1. {{FAILURE_HEADLINE}}</h3>
      </header>
      <section>
        <h4>How it played out</h4>
        <p>{{STORY}}</p>
      </section>
      <section>
        <h4>Underlying assumption</h4>
        <p>{{ASSUMPTION}}</p>
      </section>
      <section>
        <h4>Early warning signs</h4>
        <ul>{{SIGNALS_LI}}</ul>
      </section>
    </article>

  Accent palette to cycle through (one per card):
    #ff6b6b  #ffa94d  #ffd43b  #51cf66  #4dabf7  #b197fc  #f783ac  #74c0fc
-->
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>Premortem — {{TITLE}}</title>
<style>
  :root {
    --bg: #0a0e1a;
    --bg-2: #121829;
    --bg-3: #1a2138;
    --fg: #e6e9f2;
    --fg-dim: #9aa3b8;
    --rule: #232b44;
    --warn: #ffa94d;
    --danger: #ff6b6b;
    --ok: #51cf66;
  }
  * { box-sizing: border-box; }
  html, body { margin: 0; padding: 0; background: var(--bg); color: var(--fg); }
  body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Inter, sans-serif; line-height: 1.55; font-size: 16px; }
  main { max-width: 980px; margin: 0 auto; padding: 48px 24px 96px; }
  h1 { font-size: 32px; margin: 0 0 8px; letter-spacing: -0.01em; }
  h2 { font-size: 22px; margin: 40px 0 16px; letter-spacing: -0.005em; }
  h3 { font-size: 18px; margin: 0 0 8px; }
  h4 { font-size: 13px; text-transform: uppercase; letter-spacing: 0.08em; color: var(--fg-dim); margin: 16px 0 6px; font-weight: 600; }
  p { margin: 0 0 12px; }
  ul { margin: 0 0 12px; padding-left: 20px; }
  li { margin-bottom: 6px; }
  .meta { color: var(--fg-dim); font-size: 14px; margin-bottom: 32px; }
  .meta span + span::before { content: " · "; margin: 0 6px; }

  .frame {
    background: var(--bg-2);
    border-left: 3px solid var(--warn);
    padding: 16px 20px;
    border-radius: 6px;
    margin-bottom: 32px;
    font-size: 15px;
  }
  .frame strong { color: var(--warn); }

  .synthesis {
    background: var(--bg-2);
    border: 1px solid var(--rule);
    border-radius: 10px;
    padding: 24px 28px;
    margin-bottom: 40px;
  }
  .synthesis-block { padding: 16px 0; border-bottom: 1px solid var(--rule); }
  .synthesis-block:last-child { border-bottom: 0; }
  .synthesis-label { color: var(--fg-dim); font-size: 12px; text-transform: uppercase; letter-spacing: 0.1em; margin-bottom: 6px; }

  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 16px;
    margin-bottom: 32px;
  }
  .pill {
    background: var(--bg-3);
    border: 1px solid var(--rule);
    border-left: 3px solid var(--accent, var(--fg-dim));
    border-radius: 6px;
    padding: 12px 14px;
    font-size: 14px;
  }
  .pill .num { color: var(--fg-dim); font-size: 11px; letter-spacing: 0.08em; text-transform: uppercase; }

  .card {
    background: var(--bg-2);
    border: 1px solid var(--rule);
    border-left: 4px solid var(--accent, var(--danger));
    border-radius: 10px;
    padding: 20px 24px;
    margin-bottom: 16px;
  }
  .card header { display: flex; align-items: center; justify-content: space-between; gap: 12px; margin-bottom: 8px; }
  .card h3 { flex: 1; }
  .badge {
    display: inline-block;
    font-size: 11px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    padding: 3px 8px;
    border-radius: 999px;
    border: 1px solid currentColor;
    white-space: nowrap;
  }
  .severity-high   { color: var(--danger); }
  .severity-medium { color: var(--warn); }
  .severity-low    { color: var(--ok); }

  footer { color: var(--fg-dim); font-size: 13px; margin-top: 48px; padding-top: 16px; border-top: 1px solid var(--rule); }
</style>
</head>
<body>
<main>
  <h1>Premortem</h1>
  <div class="meta">
    <span>{{TITLE}}</span>
    <span>{{TIMESTAMP}}</span>
    <span>{{N_FAILURES}} failure modes investigated</span>
  </div>

  <div class="frame">
    <strong>Frame:</strong> It is 6 months from now. <strong>{{WHAT}}</strong> has failed. We are looking back to understand why.
    <div style="margin-top: 8px; color: var(--fg-dim); font-size: 14px;">
      <div><em>Audience:</em> {{WHO}}</div>
      <div><em>Success criteria:</em> {{SUCCESS}}</div>
    </div>
  </div>

  <h2>Synthesis</h2>
  <div class="synthesis">
    <div class="synthesis-block">
      <div class="synthesis-label">Most likely failure</div>
      {{MOST_LIKELY}}
    </div>
    <div class="synthesis-block">
      <div class="synthesis-label">Most dangerous failure</div>
      {{MOST_DANGEROUS}}
    </div>
    <div class="synthesis-block">
      <div class="synthesis-label">Hidden assumption</div>
      {{HIDDEN_ASSUMPTION}}
    </div>
    <div class="synthesis-block">
      <div class="synthesis-label">Revised plan</div>
      {{REVISED_PLAN}}
    </div>
    <div class="synthesis-block">
      <div class="synthesis-label">Pre-launch checklist</div>
      {{CHECKLIST}}
    </div>
  </div>

  <h2>Failure modes investigated</h2>
  <div class="grid">
    {{FAILURE_PILLS}}
  </div>

  <h2>Deep dives</h2>
  {{FAILURE_CARDS}}

  <footer>
    Premortem generated {{TIMESTAMP}} · {{N_FAILURES}} parallel investigators · subject: {{TITLE}}
  </footer>
</main>
</body>
</html>
